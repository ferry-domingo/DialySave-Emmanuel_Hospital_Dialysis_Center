{
  "name": "dialysave",
  "private": true,
  "version": "3.0.0",
  "scripts": {
    "install:all": "npm ci --prefix frontend && npm ci --omit=dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "test": "npm test --prefix backend",
    "lint": "npm run lint --prefix frontend"
  },
  "engines": {
    "node": "20.x"
  }
}
